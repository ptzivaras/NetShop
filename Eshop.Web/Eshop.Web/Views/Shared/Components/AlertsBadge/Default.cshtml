@model int

<a class="nav-link position-relative" href="#" title="Stock Alerts" onclick="alert('Stock Alerts feature coming soon!'); return false;">
    🔔 Alerts
    @if (Model > 0)
    {
        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
            @Model
            <span class="visually-hidden">unread alerts</span>
        </span>
    }
</a>
